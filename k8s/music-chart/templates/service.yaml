apiVersion: v1
kind: Service
metadata:
  name: {{ include "music-chart.fullname" . }}
  labels:
    {{- include "music-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "music-chart.selectorLabels" . | nindent 4 }}
